# Parametry aktywnych modeli używanych w ocenie rolling-origin.
global_training:
  seed: 42

xgboost:
  lags: 10
  n_estimators: 100
  max_depth: 6
  learning_rate: 0.1
  n_jobs: 1  # Jeden wątek na fit; równoległość zapewnia 20 procesów rolling-origin

sarimax:
  order: [1, 1, 1]
  seasonal_order: [1, 1, 1, 12]
