# univariate-time-series-reconstruction-framework
Framework enables to create multiple degraded datasets from source univariate time series using different missingness techniques (MNAR, MAR, MCAR). Second part of the framework offers comparison of different models in order to reconstruct the missing values. 
